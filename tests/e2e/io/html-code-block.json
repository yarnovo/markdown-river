{
  "description": "代码块中的 < 符号保留",
  "chunks": ["<code>", "if (index <", " array.length) {", " process(); }", "</code>"],
  "expectedOutputs": [
    "<code>",
    "<code>if (index <",
    "<code>if (index < array.length) {",
    "<code>if (index < array.length) { process(); }",
    "<code>if (index < array.length) { process(); }</code>"
  ]
}
